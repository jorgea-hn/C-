#include <iostream>
using namespace std;

int main(){


    return 0;
}

// Ejercicios de referencia
// - Crea una funcion que intercambie dos valores usando referencias.
// - Crea una funcion que reciba un numero por referencia y o convierta en negativo si es positivo.
// - Crea una funcion que multiplique dos numeros y almacene el resultado en una variable pasada por refenrencia.

// Ejercicios de memoria dinamica
// - Escribe un programa que cree un arreglo dinamico del tamaño que el usuario desee
// - LLena el arreglo con numeros ingresados por el usuario
// - Calcula el promedio, encuentra el mayor y el menor y libera la memoria al final.

// Buenas practicas
// - Escribe una funcion que reciba un arreglo dinamico, lo llene con valores aleatorios entre 1 y 100 y luego libere la memoria.
// - Implemente validaciones que verifiquen que el puntero no sea nullprt antes de usarlo.
// - Usa una funcion que reciba un puntero y lo libere correctamente usando una referencia al puntero.

// haz que el programa repita el proceso hasta que el usuario indique que desea salir - al ejercicio pratico haccer un menu
// agrega validacion para evitar errores al ingresar datos no validos
// implemente una funcion adicional que ordene el arreglo antes de mostrarlo